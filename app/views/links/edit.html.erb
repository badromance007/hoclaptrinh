<%= provide(:current_title, @link.title ) %>
<%= provide(:current_description, @link.description ) %>

<h1>Editing Link</h1>

<%= render 'form' %>

<%= link_to 'Show', @link %> |
<%= link_to 'Back', links_path %>
